# Prompt for Claude Code — take "table-system-v2 + width/focus shell" live

> Paste everything below the line into Claude Code, running at the root of the `UIx` repo.

---

You are working in the **`uix-styleguide` monorepo** (UIx v2, `master`): `@tensor_1/tokens` (DTCG tokens → CSS/Tailwind/TS, plus the `.uix-*` component CSS) and `@tensor_1/react` (thin React wrappers). This is the house design system consumed by **Tensor / POSx / SHOPx**; products consume it, they never fork.

A prior session landed a large feature on `master` — **"table-system-v2 + width/focus app-shell"** — but ran in a sandbox that could **not** `npm install`, so the token/CSS layer was validated but the **React layer, the real build, and the visual/a11y suites were never run**. Your job: **verify it, fix whatever the gates catch, complete the remaining follow-ups, and take it live.**

## Starting point — already changed, do NOT redo

**Verified green** against the real gates (`test:parity`, `test:contract`) using staged copies:

- `packages/tokens/tokens/base/size.json` — new token `--uix-sidebar-w-rail: 56px`.
- `packages/tokens/tests/tokens.baseline.css` — updated to include it (parity baseline).
- `packages/tokens/build/**` — token + style artifacts regenerated by a hand-written **faithful port** of `style-dictionary.config.mjs` (fidelity-diffed vs. the prior committed output: additions only).
- `packages/tokens/styles/components/table.css` — full interaction layer added, fully tokenized.
- `packages/tokens/styles/components/app-shell.css` — three-tier nav + focus mode + full-bleed.
- `packages/tokens/styles/components/sidebar.css` — rail width now uses `--uix-sidebar-w-rail`.
- `packages/tokens/styles/components/peek.css` — restored (intent unchanged).

**Delivered but UNVERIFIED** (prior env lacked `typescript`/`tsup`/`api-extractor`/`playwright`):

- `packages/react/src/table-engine.ts` — NEW framework-agnostic engine. Logic was node-tested (25/25) but not typechecked or built.
- `packages/react/src/components/Table.tsx` — new subcomponents `BulkBar`, `RowActions`, `RowAction`, `ExpandToggle`, `CellStrong`, `CellSub`, `Mark`, `Highlighted`; `Table` gained `fixed`; `Th` gained `sortOrder`.
- `packages/react/src/components/AppShell.tsx` — `nav` / `focus` / `onExitFocus` / `mainBleed` props (`collapsed` kept as a back-compat alias).
- `packages/react/src/index.ts` — exports the above + `export * from './table-engine.js'`.
- `.changeset/table-system-v2.md` — minor bump for both packages.

**New CSS classes** (needed for the demos): `.uix-shell[data-nav="full|rail|hidden"]`, `.uix-shell[data-focus]`, `.uix-shell__main--bleed`, `.uix-shell__focus-exit`; `.uix-table__selcol/__selcell`, `.uix-bulkbar` (`__count`/`__spacer`), `.uix-table__actions`, `.uix-rowact`, `.uix-table__expand`, `.uix-table__detail`/`__detail-inner`, `.uix-kv` (`__k`/`__v`), `.uix-cell-edit` (`__pencil`/`__ok`), `.uix-table__resize`, `.uix-mark`, `.uix-cell-strong`/`-sub`/`-mono`, `.uix-table--priority[-mid]`, `.uix-table--cards`, `.uix-compare` (`__head`/`__row`/`__attr`/`__cell`), `.uix-col--w-*`/`--flex`/`--truncate`/`--num`, `.uix-table--fixed`.

## Guardrails (do not violate)

- **The `--uix-*` contract is frozen and parity-gated.** Any token change edits `tokens/*.json` **and** `tests/tokens.baseline.css` in the same commit, with a dark value where the token overrides in dark.
- **Component CSS stays fully tokenized.** `test:contract` rejects raw hex/rgb/hsl colors, raw `z-index`, and raw `px` in spacing/radius/type properties. Raw `px` is allowed only in geometry properties; if you genuinely need another, add a justified entry to `packages/tokens/tests/raw-value-allowlist.json`.
- **Additive and non-breaking.** Keep `data-collapsed` (CSS) and `collapsed` (React) working.
- **No forking.** Everything lives in UIx.

## Tasks (in order)

### 1 — Verify the foundation
- `npm install`
- `npm run build:all` (real style-dictionary + tsup). Then `git diff --stat packages/tokens/build` — the committed token artifacts should be unchanged (the prior port was faithful). If they differ, the real build is source of truth: keep it, re-run parity.
- `npm run test:parity` and `npm run test:contract` — must be green. Fix anything red.

### 2 — React: typecheck, build, API report
- `npm run typecheck -w @tensor_1/react` — fix type errors in `table-engine.ts`, `Table.tsx`, `AppShell.tsx`. Confirm the `highlightSegments` import path (`../table-engine.js`) resolves and all `import type` usages are correct.
- `npm run build:react` — must build clean.
- `npm run test:api:update`, then `npm run test:api` — regenerate and commit `etc/uix-react.api.md` for the new exports.
- Add a unit test for the engine (precedent: `packages/tokens/guide/app.test.js`). Cover `multiSort`, `applyFilters` (every op), `searchRows`/`highlightSegments`, `serializeView`/`parseView` round-trip, `virtualWindow`, `reorder`, and selection helpers — including that `mergePinned` hoists pinned rows above filtered ones.

### 3 — Styleguide demos (the main missing piece)
Make the styleguide reflect what now ships (keep the vanilla-JS harness pattern; `--uix-*` only):
- `packages/tokens/index.html` — add the **width/focus shell** demo: a `.uix-shell` with a `data-nav` segmented control (full/rail/hidden), a Focus-mode toggle (`data-focus`, `Esc` exits), and a full-bleed switch.
- `packages/tokens/tables.html` — wire the shipped patterns into the live grids (selection + bulk bar, row actions/kebab, expandable rows, inline edit, column resize, search highlight, responsive ladder, compare) and flip the "PROPOSED" annotations to shipped.
- `packages/tokens/guide/app.js` — implement the interactions (selection state, multi-sort, resize drag, column show/hide, search, `data-nav`/`data-focus` toggles with persistence + `Esc`). Reuse the engine's algorithms so behavior matches the React layer.
- Then `npm run test:visual:update` (screenshots will change — review the diffs) and `npm run test:a11y` (fix violations).

### 4 — Docs
- Add a **"primitives to consume, not fork"** list to the `@tensor_1/tokens` README (the U5 doc half): the table primitives + shell nav downstreams should consume, with the token each is themed by.
- Update `Docs/design-system.md`: add the new Table subcomponents, `AppShell` `nav`/`focus`, and `table-engine` to the catalog.

### 5 — (Optional) `useTable` hook
If useful, add a `useTable` hook to `@tensor_1/react` that composes the engine into selection/sort/filter/search/view state; export it and re-run `test:api:update`.

### 6 — Finalize
- Confirm `.changeset/table-system-v2.md` is accurate (both packages, minor).
- Run the full suite: `npm run test:parity && npm run test:contract && npm run test:api && npm run test:smoke && npm run test:visual && npm run test:a11y`.
- Commit. Then note (do not execute) the downstream step: **Tensor re-syncs UIx and replaces its local `data-table.tsx` fork** with the shipped `Table` + `table-engine` — that adoption is the whole point of shipping this centrally.

When done, report exactly what you changed from the delivered state, the final gate results, and any decisions you had to make.
