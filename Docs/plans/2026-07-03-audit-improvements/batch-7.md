# Batch 7 — SD v5 readiness · roadmap guard · status badges · budgets · label · shadcn

*Depends on Batch 6 (merged). Paste below the line into a fresh agent. This is the higher-risk tail —
DTCG-3 is the only slice that can touch `--uix-*` output; treat its parity check as a tripwire.*

---

Work in `E:/Development/Projects/UIx` (clean `master`). Branch:
`git checkout master && git pull && git checkout -b batch7-sdv5-badges-release`. Assume Batch 0–6 landed (all
wrapper batches A/B/C so every badge target exists; PERF-1/PERF-2 size gates; DTCG-2 validity gate + ADR-0019;
A11Y-2 registry; GOV-3 contract-guard + reviewer-policy; DIST-2 registry). **GATE CHECK:** DIST-5 builds ONLY if
ADR-0017 chose **option B** (shadcn registry); if A or C, report it cancelled and skip. `ci.yml` writer = BREADTH-5;
`release.yml` writer = PERF-4; `index.html`/guide = A11Y-5; `contract-change-process.md` = GOV-5.

### DTCG-3 — Style Dictionary v4→v5 readiness (HIGHEST parity risk — verify FIRST, in isolation)  *(dep DTCG-2)*
Either bump `style-dictionary` devDep to `^5` in `packages/tokens/package.json` and migrate
`style-dictionary.config.mjs` so `npm run build` emits **byte-identical** `build/css/tokens.css` +
`build/tailwind/{theme.css,preset.cjs}` + `build/ts/{tokens.js,tokens.d.ts}` (the `uix/name` + identity `uix/value`
transforms must behave unchanged — `token.original.$value` read verbatim), OR — if v5 moves the value/transform
pipeline in a way that would change `--uix-*` output — record a justified **NO-GO** in
`Docs/2026-07-03-style-dictionary-v5-migration.md` with the exact blocking v5 API deltas (getPlatformTokens/
hasInitialized, transform registration, `token.original` access) and KEEP the devDep at `^4`. Create
`.changeset/dtcg-sd-v5-readiness.md` classifying it "tooling-only, no `--uix-*` change". **MUST NOT edit
`tests/tokens.baseline.css` to force parity** — that is the tripwire. **Verify (run this alone, first):**
`npm run build && npm run test:parity && npm run test:contract` all green; `git diff --stat build/` shows ZERO
changed generated bytes (or the doc explains an approved no-go and the devDep stays `^4`); `build:all` succeeds.
**If parity fails, STOP — do not make parity pass by editing the baseline.**

### BREADTH-5 — Roadmap-coverage guard  *(dep BREADTH-1, BREADTH-4)*
Create `packages/tokens/scripts/check-roadmap-coverage.mjs` (list `styles/components/*.css`, parse component names
from `Docs/component-roadmap.md`, exit non-zero listing any css file missing a row and any row with no css file;
pin the parser to BREADTH-1's table format in a comment). Add root `test:roadmap`. Add a single step to the
EXISTING `gates` job in `ci.yml` running `npm run test:roadmap`. **Verify:** `node check-roadmap-coverage.mjs`
exits 0 on the complete roadmap, non-zero when a row is removed or a dummy css file is added; new CI step green.

### A11Y-5 — Styleguide maturity badges (read from the registry)  *(dep A11Y-2, BREADTH-4)*
Create `packages/tokens/guide/status.js` (PURE helper: badge label/class from a status value, unit-tested in
`status.test.js` under `node:test`, matching `guide/app.test.js`) + `status.test.js`. The styleguide reads a
committed mirror of `packages/react/component-status.json` (a small JS/JSON file in `guide/`, or a build step that
copies it — **NO network fetch**, keep the build-free/no-flash invariant) and renders a Draft/Beta/Stable badge on
each demo block in `packages/tokens/index.html`, styled with existing `--uix-*` tokens (accessible TEXT, not
color-only) in `guide/guide.css`. **Verify:** `npm run build` then `npm run test:a11y` (styleguide green with
badges, light+dark); `node --test guide/status.test.js` green; badges render on `index.html`. **VR (correction C4):**
badges change rendering → re-baseline the `*-index-linux.png` goldens via the pinned container /
`update-visual-goldens`; do not hand-edit PNGs or assert local `test:visual` on Windows.

### PERF-4 — Published budgets doc (+ release lockstep is automatic now)  *(dep PERF-1, PERF-2)*
Create `Docs/performance-budgets.md`: each budget (main React entry, `./chart` entry, per-CSS-bundle totals) with
current measured value, committed limit, head-room rationale, and the re-measure/update commands (`test:size`,
`test:size:css`/`size:css:update`); explicitly scope OUT Core-Web-Vitals as future work. **Because GOV-6 made
`release.yml` reuse `ci.yml`, the size job already gates publishing — do NOT hand-mirror steps into release.yml**
(that hand-copy is gone; correction C3). Instead, verify the reuse covers the `size` job and note it in the doc.
Link the doc from README. **Verify:** the `size` job runs on a tag build via the reusable-workflow call; doc numbers
match `.size-limit.cjs` + `css-size.baseline.json`.

### GOV-5 — Contract-change label + canonical path list  *(dep GOV-3)*
Create `.github/labels.yml` defining the `contract-change` label (name/color/description) reproducible via
`gh label create -F` or a label-sync action (so the label GOV-3's gate requires actually exists). Add a
"Canonical contract paths" block to `Docs/contract-change-process.md` — the authoritative **narrowed** list
(`tokens/`, `themes/`, `tests/tokens.baseline.css`, `style-dictionary.config.mjs`, and
`scripts/{build-styles,build-themes,check-parity,check-contract}.mjs`, correction C5) — noting that CODEOWNERS,
`contract-guard.yml`, `scripts/check-contract-diff.mjs`, and `reviewer-policy.md` all mirror it; the list MUST
byte-match GOV-3's script. Additive only. **Verify:** `gh label create -F .github/labels.yml` creates the label;
`grep` confirms the same 5 paths in `contract-change-process.md` and `scripts/check-contract-diff.mjs`.

### DIST-5 — shadcn-CLI-compatible per-component registry JSON (CONDITIONAL on ADR-0017 = option B)  *(dep DIST-2)*
Create `packages/tokens/scripts/build-shadcn-registry.mjs` (derive per-component shadcn registry-item JSON from
`registry/registry.json`: `registry/index.json` + `registry/styles/<name>.json` for a proven handful — `$schema`,
`name`, `type`, `files[]` with inlined CSS, `cssVars`), `registry/index.json`, `registry/styles/button.json`. Add
`build:shadcn` to the tokens build chain, GATED to no-op with a clear message if ADR-0017 chose A/C. Add a parity
check asserting embedded CSS byte-matches on-disk `styles/components/button.css`; ensure `index.json` can't drift.
Add the `build:shadcn` key to `packages/tokens/package.json` **after DTCG-3's devDep edit** to avoid a conflict.
**Verify:** `npm run build` + validate `registry/styles/button.json` against the shadcn registry-item schema;
embedded CSS byte-matches `button.css`. **CANCELLED if ADR-0017 ≠ option B.**

### WHEN DONE
`npm run build:all`, `test:parity` (CRITICAL — DTCG-3 must not move bytes), `test:contract`, `test:api`,
`test:smoke`, `test:a11y`, `test:size`, `test:size:css`, `test:roadmap` green; re-baseline VR goldens for the
A11Y-5 badge change. Add changesets: DTCG-3's SD-v5 changeset (tooling-only), plus any `@tensor_1/tokens`
surface additions (shadcn registry). Commit and push. **If DTCG-3's parity check fails, treat it as the intended
tripwire — do not edit `tokens.baseline.css`.**
