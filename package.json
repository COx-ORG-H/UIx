{
  "name": "uix-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "UIx v2 — private workspace root. Publishable packages live in packages/* (@uix/tokens, @uix/react). Source of the --uix-* contract (ADR-0016).",
  "type": "module",
  "license": "UNLICENSED",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @uix/tokens",
    "build:react": "cd packages/react && npm ci && npm run build",
    "build:all": "npm run build && npm run build:react",
    "test:parity": "npm run test:parity -w @uix/tokens",
    "test:contract": "npm run test:contract -w @uix/tokens",
    "test:api": "api-extractor run --config packages/react/api-extractor.json && api-extractor run --config packages/react/api-extractor.chart.json",
    "test:api:update": "api-extractor run --local --config packages/react/api-extractor.json && api-extractor run --local --config packages/react/api-extractor.chart.json",
    "test:smoke": "node tests/smoke-consumer/run.mjs",
    "serve:styleguide": "serve -l 4178 .",
    "test:visual": "playwright test",
    "test:visual:update": "playwright test --update-snapshots",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "@microsoft/api-extractor": "^7.58.9",
    "@playwright/test": "^1.61.0",
    "serve": "^14.2.6"
  }
}
