---
"@tensor_1/react": minor
---

Add breadth batch C React wrappers (completing presentational coverage): `ContactCard` +
`ContactCardStat` (composes `Button`), `Pipeline` + `PipelineStage`, `Flow` + `FlowChip`,
`Media`, and a stateless `Lightbox`.
