## API Report File for "@tensor_1/react"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import * as react from 'react';
import { ReactElement } from 'react';

// @public (undocumented)
export type Align = 'start' | 'center' | 'end';

// @public
export function buildEmojiData(locale: EmojiLocale, compact: ReadonlyArray<CompactEmoji>, messages: EmojiMessages): EmojiData;

// @public
export function canRenderEmoji(emoji: string): boolean;

// @public
export interface CompactEmoji {
    // (undocumented)
    group?: number;
    // (undocumented)
    label: string;
    // (undocumented)
    order?: number;
    // (undocumented)
    tags?: string[];
    // (undocumented)
    unicode: string;
}

// @public (undocumented)
export const DEFAULT_EMOJI_PICKER_LABELS: EmojiPickerLabels;

// @public (undocumented)
export const DEFAULT_QUICK_REACTIONS: ReadonlyArray<string>;

// @public (undocumented)
export const DEFAULT_REACTION_BAR_LABELS: ReactionBarLabels;

// @public
export const EMOJI_GRID_COLUMNS = 8;

// @public (undocumented)
export interface EmojiData {
    // (undocumented)
    emojis: ReadonlyArray<EmojiEntry>;
    // (undocumented)
    groups: ReadonlyArray<EmojiGroup>;
    // (undocumented)
    locale: EmojiLocale;
    names: ReadonlyMap<string, string>;
}

// @public (undocumented)
export type EmojiDataLoader = (locale: EmojiLocale) => Promise<EmojiData>;

// @public (undocumented)
export interface EmojiEntry {
    emoji: string;
    group: number;
    name: string;
    tags: ReadonlyArray<string>;
}

// @public (undocumented)
export interface EmojiGroup {
    // (undocumented)
    key: string;
    name: string;
}

// @public
export function emojiImageFileName(emoji: string): string;

// @public (undocumented)
export type EmojiLocale = 'en' | 'de';

// @public
export interface EmojiMessages {
    // (undocumented)
    groups: ReadonlyArray<{
        key: string;
        message: string;
        order: number;
    }>;
}

// @public
export function EmojiPicker(input: EmojiPickerProps): react.JSX.Element;

// @public (undocumented)
export interface EmojiPickerLabels {
    categories: string;
    dialog: string;
    loadFailed: string;
    loading: string;
    noResults: string;
    quickPicks: string;
    recent: string;
    search: string;
}

// @public (undocumented)
export interface EmojiPickerProps {
    // (undocumented)
    className?: string;
    emojiImageBaseUrl?: string;
    // (undocumented)
    labels?: Partial<EmojiPickerLabels>;
    loadData?: EmojiDataLoader;
    locale?: EmojiLocale;
    // (undocumented)
    onOpenChange?: (open: boolean) => void;
    onSelect: (emoji: string) => void;
    // (undocumented)
    placement?: Placement;
    quickPicks?: ReadonlyArray<string>;
    recentStorageKey?: string;
    trigger: ReactElement;
}

// @public
export const loadEmojiData: EmojiDataLoader;

// @public
export function normalizeEmojiImageBaseUrl(value: string | undefined): string | undefined;

// @public (undocumented)
export type Placement = Side | `${Side}-${Align}`;

// @public
export function ReactionBar(input: ReactionBarProps): react.JSX.Element;

// @public (undocumented)
export interface ReactionBarLabels {
    addReaction: string;
    group: string;
    others: string;
    reactedCount: string;
    reactedWith: string;
}

// @public (undocumented)
export interface ReactionBarProps {
    // (undocumented)
    className?: string;
    disabled?: boolean;
    emojiImageBaseUrl?: string;
    // (undocumented)
    labels?: Partial<ReactionBarLabels>;
    // (undocumented)
    loadData?: EmojiDataLoader;
    locale?: EmojiLocale;
    maxNames?: number;
    onToggle: (emoji: string) => void;
    pickerLabels?: Partial<EmojiPickerLabels>;
    quickPicks?: ReadonlyArray<string>;
    // (undocumented)
    reactions: ReadonlyArray<ReactionSummary>;
}

// @public (undocumented)
export interface ReactionSummary {
    // (undocumented)
    count: number;
    emoji: string;
    names: ReadonlyArray<string>;
    reactedByMe: boolean;
}

// @public
export function searchEmoji(data: EmojiData, query: string, limit?: number): EmojiEntry[];

// @public
export type Side = 'top' | 'bottom' | 'left' | 'right';

// (No @packageDocumentation comment for this package)

```
