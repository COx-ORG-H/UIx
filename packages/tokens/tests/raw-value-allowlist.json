{
  "_note": "Justified raw values in component CSS (ADR-0016 Decision 2, 'full strict'). check-contract.mjs reads this: each entry exempts ONE (file, prop, value) from the raw-value gate WITH a written reason. These are component-INTRINSIC geometry — concentric radii, hairline pads, icon clearances, thumb centering — not --uix-* contract-scale values; minting tokens for them would pollute the namespace. An unlisted raw value still FAILS the gate (Law 1). A stale entry (no longer present in the CSS) prints a cleanup warning. Match is on whitespace-normalized prop+value.",
  "entries": [
    { "file": "input.css", "prop": "padding-left", "value": "38px", "why": "Clearance for the absolutely-positioned leading icon (icon at left:10px + 20px glyph + gap). A component-intrinsic layout dimension, not a spacing-scale step." },
    { "file": "input.css", "prop": "padding-right", "value": "38px", "why": "Clearance for the absolutely-positioned trailing icon (mirror of the leading-icon clearance). Component-intrinsic layout dimension." },
    { "file": "labels.css", "prop": "padding", "value": "1px var(--uix-space-2)", "why": "Hairline vertical pad on an inline label chip — 1px sits below the 4px space scale; the chip's height is set optically, this is a sub-scale nudge. Horizontal pad is already tokenized." },
    { "file": "prose.css", "prop": "padding", "value": "1px var(--uix-space-2)", "why": "Hairline vertical pad on inline <code> — 1px is a sub-scale optical nudge below the 4px space scale. Horizontal pad is already tokenized." },
    { "file": "reactions.css", "prop": "padding", "value": "1px var(--uix-space-2)", "why": "Hairline vertical pad on a reaction pill — line-height:1.7 does the real vertical sizing; 1px is a sub-scale optical nudge. Horizontal pad is already tokenized." },
    { "file": "reactions.css", "prop": "font-size", "value": "18px", "why": "Emoji-glyph display size in the reaction picker — sizing a pictographic glyph (icon-class), deliberately off the text type scale (which tops out at 40px display / bottoms at 11px eyebrow)." },
    { "file": "segmented.css", "prop": "border-radius", "value": "calc(var(--uix-radius-md) - 3px)", "why": "Concentric inner radius: option corner = container --uix-radius-md minus the 1px border + space-1 inset (~3px). A geometric relationship to the parent's radius, not an independent scale value." },
    { "file": "tabs.css", "prop": "border-radius", "value": "calc(var(--uix-radius-md) - 3px)", "why": "Concentric inner radius for a pill tab (same construction as segmented): container --uix-radius-md minus the padding inset. Geometric relationship to the parent, not a scale value." },
    { "file": "tabs.css", "prop": "margin-bottom", "value": "-1px", "why": "Pulls the enclosed tab down to overlap the container's 1px bottom border so the selected tab's border merges with it (the classic enclosed-tab border-collapse). Tied to the 1px border width." },
    { "file": "slider.css", "prop": "margin-top", "value": "-6px", "why": "Centers the 18px thumb on the 6px track: -(18 - 6) / 2 = -6px. Intrinsic to the thumb/track geometry; changing either dimension changes this." },
    { "file": "toast.css", "prop": "margin-top", "value": "1px", "why": "1px optical nudge aligning the status icon with the title's cap height. Sub-scale hairline, below the 4px space scale." },
    { "file": "editorial-home.css", "prop": "font-size", "value": "clamp(30px, 4vw, 48px)", "why": "Fluid editorial page-title ramp ported 1:1 from the approved TENSOR intranet prototype — the endpoints are viewport-interpolation bounds of a display ramp, not fixed type-scale steps (scale tops out at --uix-text-display 40px)." },
    { "file": "editorial-home.css", "prop": "font-size", "value": "clamp(26px, 3vw, 38px)", "why": "Fluid featured-story title ramp (same construction as the page-title ramp): viewport-interpolation bounds of an editorial display ramp, intentionally between --uix-text-h2 and --uix-text-display." },
    { "file": "editorial-home.css", "prop": "font-size", "value": "48px", "why": "Story-signal hero numeral — an editorial display size above --uix-text-display (40px); the 620px breakpoint steps it down onto the scale (var(--uix-text-display))." },
    { "file": "editorial-home.css", "prop": "font-size", "value": "18px", "why": "Editorial card-heading size (rundown/rail-card titles, event-date day) — the fixed type scale jumps 15px (h3) → 24px (h2); heading-family at regular weight needs the intermediate step for hierarchy. Ported 1:1 from the approved TENSOR prototype." },
    { "file": "editorial-home.css", "prop": "font-size", "value": "20px", "why": "Stat-line numeral — data callout between --uix-text-h3 (15px) and --uix-text-h2 (24px); tabular-nums display figure, not a text-scale step. Ported 1:1 from the approved TENSOR prototype." },
    { "file": "editorial-home.css", "prop": "padding-left", "value": "42px", "why": "Clearance for the absolutely-positioned search icon in .uix-searchbar (icon at left:14px + 17px glyph + gap) — same construction as the input.css leading-icon clearance. Component-intrinsic layout dimension." }
  ]
}
