{
  "name": "@uix/tokens",
  "version": "1.0.0",
  "description": "House design-system token contract: pure CSS custom properties (light+dark), shadcn bridge, Tailwind bindings (required for @uix composites, optional for tokens-only consumers), and the uix-lint-tokens gate. NOTE: @uix scope is a placeholder until the npm scope is claimed (see ADR-0004). Tailwind CSS v4+ only.",
  "license": "UNLICENSED",
  "type": "module",
  "engines": { "node": ">=22" },
  "files": ["tokens.css", "shadcn-bridge.css", "tailwind.css", "theme-contract.json", "bin", "themes"],
  "exports": {
    "./tokens.css": "./tokens.css",
    "./shadcn-bridge.css": "./shadcn-bridge.css",
    "./tailwind.css": "./tailwind.css",
    "./theme-contract.json": "./theme-contract.json",
    "./themes/*.css": "./themes/*.css"
  },
  "bin": {
    "uix-lint-tokens": "bin/uix-lint-tokens.mjs"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "prepublishOnly": "node scripts/build.mjs && node ../../scripts/check-token-semver.mjs"
  }
}
